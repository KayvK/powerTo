#include "main.h"

// TODO learn to replace namespaces
int main(){
    ldouble exponent, base = 0;
    cout << "enter an exponent and a base: ";
    cin >> exponent >> base;
    cout << powerTo(exponent, base);
}
